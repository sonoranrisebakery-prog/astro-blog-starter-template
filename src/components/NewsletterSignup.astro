---
// MailerLite embedded form (form id 192604569655576215, account 2494870).
// Markup/classes below are kept as MailerLite's webforms.min.js expects them
// so its AJAX submit + inline success swap keep working; only the presentation
// has been rewritten to match the site's own styles instead of the vendor CSS.
---

<div id="mlb2-43601852" class="ml-form-embedContainer newsletter-signup">
	<div class="ml-form-embedWrapper row-form">
		<p class="newsletter-copy">
			Follow our journey from freshly milled grain to the first Sonoran Rise loaves. Get updates
			on product development, launch progress, and availability.
		</p>

		<form
			class="ml-block-form newsletter-form"
			action="https://assets.mailerlite.com/jsonp/2494870/forms/192604569655576215/subscribe"
			method="post"
			target="_blank"
		>
			<div class="newsletter-row">
				<label class="sr-only" for="newsletter-email">Email</label>
				<input
					id="newsletter-email"
					type="email"
					name="fields[email]"
					placeholder="Email"
					autocomplete="email"
					required
				/>
				<button type="submit">Subscribe</button>
				<button disabled style="display: none;" type="button" class="loading">
					<span class="sr-only">Loading...</span>
				</button>
			</div>

			<label class="newsletter-checkbox">
				<input type="checkbox" required />
				<span>Opt in to receive news and updates.</span>
			</label>

			<input type="hidden" name="ml-submit" value="1" />
			<input type="hidden" name="anticsrf" value="true" />
		</form>
	</div>

	<div class="ml-form-embedWrapper row-success" style="display: none">
		<h4>Thank you!</h4>
		<p>You have successfully joined our subscriber list.</p>
	</div>
</div>

<style>
	.newsletter-signup {
		max-width: 480px;
		margin: 2em 0;
	}
	.newsletter-copy {
		margin-bottom: 1em;
	}
	.newsletter-row {
		display: flex;
		gap: 0.5em;
	}
	.newsletter-row input[type='email'] {
		flex: 1;
		padding: 0.6em 0.75em;
		border: 1px solid rgb(var(--gray));
		border-radius: 4px;
		font-family: inherit;
		font-size: 16px;
	}
	.newsletter-row button[type='submit'] {
		padding: 0.6em 1.25em;
		background-color: var(--accent);
		color: #fff;
		border: none;
		border-radius: 4px;
		font-family: inherit;
		font-weight: 700;
		cursor: pointer;
	}
	.newsletter-row button[type='submit']:hover {
		background-color: var(--accent-dark);
	}
	.newsletter-checkbox {
		display: flex;
		align-items: flex-start;
		gap: 0.5em;
		margin-top: 0.75em;
		font-size: 0.85em;
		color: rgb(var(--gray-dark));
	}
	.newsletter-checkbox input {
		margin-top: 0.2em;
	}
	.row-success h4 {
		margin-bottom: 0.25em;
	}
	@media (max-width: 480px) {
		.newsletter-row {
			flex-direction: column;
		}
	}
</style>

<script>
	function ml_webform_success_43601852() {
		document.querySelectorAll('#mlb2-43601852 .row-success').forEach((el) => {
			(el as HTMLElement).style.display = '';
		});
		document.querySelectorAll('#mlb2-43601852 .row-form').forEach((el) => {
			(el as HTMLElement).style.display = 'none';
		});
	}
	// @ts-expect-error - MailerLite's webforms.min.js calls this by name via JSONP.
	window.ml_webform_success_43601852 = ml_webform_success_43601852;
</script>
<script src="https://groot.mailerlite.com/js/w/webforms.min.js?v83147fa8ce2d95cb73ece7f28b469519"
></script>
<script>
	fetch('https://assets.mailerlite.com/jsonp/2494870/forms/192604569655576215/takel');
</script>
